version: '3.9'

services:
  vuex-shop:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    container_name: vuex-shop-app
    environment:
      - NODE_ENV=production
    restart: unless-stopped
